{
  "name": "ts-plugin",
  "version": "1.0.0",
  "description": "Local TS3 ClientQuery agent - queries bot via your TS3 client and pushes results to the dashboard",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "npx tsx src/index.ts"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.0"
  }
}
